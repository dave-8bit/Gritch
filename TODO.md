# Phase 3.1 Implementation Steps

- [x] 1. Read and understand all relevant files (explain.ts, prompts.ts, profile.ts, all detector types)
- [ ] 2. Create `src/ai/profile-context.ts` — pure formatter: RepositoryProfile → plain text
- [ ] 3. Modify `src/ai/prompts.ts` — add optional `repoContext` param to `explainUserPrompt()`
- [ ] 4. Modify `src/commands/explain.ts` — wire in `inspectRepository()` + `buildRepositoryContext()`
- [ ] 5. Verify TypeScript compilation
- [ ] 6. Run tests

